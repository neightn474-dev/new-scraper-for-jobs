{
  "title": "Hiring intelligence input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "adzunaAppId": {
      "title": "Adzuna App ID",
      "type": "string",
      "description": "Required Adzuna API application ID. Get your own credentials from Adzuna.",
      "editor": "textfield"
    },
    "adzunaAppKey": {
      "title": "Adzuna App Key",
      "type": "string",
      "description": "Required Adzuna API key. This actor does not ship with shared credentials.",
      "editor": "textfield",
      "isSecret": true
    },
    "countries": {
      "title": "Countries",
      "type": "array",
      "description": "Adzuna country codes to search. Keep this limited to the United States and Canada for this dataset.",
      "default": [
        "us",
        "ca"
      ],
      "prefill": [
        "us",
        "ca"
      ],
      "editor": "stringList"
    },
    "searchQueries": {
      "title": "Search queries",
      "type": "array",
      "description": "Role or hiring searches to run against Adzuna.",
      "default": [
        "software engineer",
        "sales",
        "operations",
        "customer success",
        "marketing",
        "finance",
        "data analyst"
      ],
      "editor": "stringList"
    },
    "maxPagesPerQuery": {
      "title": "Max pages per query/country",
      "type": "integer",
      "description": "Caps broad discovery volume to preserve quality and API budget.",
      "default": 2,
      "minimum": 1,
      "maximum": 20
    },
    "resultsPerPage": {
      "title": "Results per page",
      "type": "integer",
      "default": 50,
      "minimum": 1,
      "maximum": 50
    },
    "maxJobAgeDays": {
      "title": "Maximum job age in days",
      "type": "integer",
      "description": "Stale jobs older than this threshold are excluded.",
      "default": 30,
      "minimum": 1,
      "maximum": 90
    },
    "includeMediumConfidence": {
      "title": "Include medium confidence rows",
      "type": "boolean",
      "default": true
    },
    "enablePublicEnrichment": {
      "title": "Enable public web enrichment",
      "type": "boolean",
      "description": "Use no-key public web resources to discover/validate official websites, descriptions, careers evidence, and employee-size evidence.",
      "default": true
    },
    "maxEnrichmentPages": {
      "title": "Max public pages per company",
      "type": "integer",
      "description": "Limits no-key public web enrichment requests per discovered company.",
      "default": 5,
      "minimum": 1,
      "maximum": 5
    },
    "greenhouseBoardTokens": {
      "title": "Greenhouse board tokens",
      "type": "array",
      "description": "Optional no-key Greenhouse public board tokens to ingest alongside Adzuna, e.g. company slugs used in boards-api.greenhouse.io URLs.",
      "default": [],
      "editor": "stringList"
    },
    "leverCompanySlugs": {
      "title": "Lever company slugs",
      "type": "array",
      "description": "Optional no-key Lever company slugs to ingest alongside Adzuna, e.g. slugs used in api.lever.co public postings URLs.",
      "default": [],
      "editor": "stringList"
    }
  },
  "required": [
    "adzunaAppId",
    "adzunaAppKey"
  ]
}
